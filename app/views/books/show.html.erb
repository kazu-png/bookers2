<%= render 'users/info', user: @user %>
<%= render 'books/new', book: @book %><h2>Book detail</h2>
<table>
    <tr>
      <td>
        <%= link_to user_path(@new_book.user.id) do %>
        <%= attachment_image_tag @new_book.user, :profile_image, size:"40x40", format:'jpeg', fallback:"no_image.jpg" %>
        <%= @new_book.user.name %>
        <% end %>
      </td>
      <td><%= @new_book.title %></td>
      <td><%= @new_book.body %></td>
       <% if @new_book.user.id == current_user.id %>
      <td><%= link_to 'Edit', edit_book_path(@new_book.id) %></td>
      <td><%= link_to 'Destroy', book_path, method: :delete %></td>
       <% end %>
    </tr>
</table>