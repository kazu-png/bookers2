<h2>New book</h2>
 <%= form_with model:@book, local:true do |f| %>
    <%= f.label :title, "Title" %>
    <%= f.text_field :title %>

    <%= f.label :body, "Opinion" %>
    <%= f.text_area :body %>

    <%= f.submit 'Create Book' %>
<% end %>